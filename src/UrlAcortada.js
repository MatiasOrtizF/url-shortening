export default function UrlAcortada (props) {
    return(
        <div className="url-acortada">
                <h4 id="acortada">{props.acortada}</h4>
                <button className="" id="copy">Copy</button>
        </div>
    )
} 